import React from 'react';
import ReactDOM from 'react-dom';
import MiComponente from '/MiComponente'; // Importa tu componente

ReactDOM.render(
  <React.StrictMode>
    <MiComponente />
  </React.StrictMode>,
  document.getElementById('root')
)